'use client'

import CutoutTool from '@frontend/features/tools/crop/CutoutTool'

/** Herramienta "Quitar fondo" como pantalla propia (ya no es pestaña de Recorte). */
export default function CutoutScreen() {
  return <CutoutTool />
}
