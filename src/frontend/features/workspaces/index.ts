// Punto de entrada de los workspaces: importar este módulo registra todos los
// plugins de tipo (side-effect). Único sitio que enumera los tipos disponibles.
// Agregar un tipo = crear su carpeta + añadir un import aquí.

import './libre'
import './carnaval'
