'use client'

// Registra los plugins de workspace (side-effect) para la landing data-driven.
import '@frontend/features/workspaces'

export { default } from '@frontend/features/workspaces/shared/screens/WorkspacesScreen'
